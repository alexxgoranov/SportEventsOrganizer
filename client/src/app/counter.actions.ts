import { createAction } from "@ngrx/store";

export const increment = createAction('[App-navbar Component] Increment');
export const decrement = createAction('[App-navbar Component] Decrement');
export const reset = createAction('[App-navbar Component] Reset');

